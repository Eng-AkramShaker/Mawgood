import 'package:flutter/material.dart';

class Empty_Screen extends StatelessWidget {
  const Empty_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Column(
      children: [
        Container(),
      ],
    ));
  }
}
